# راهنمای افزایش فضای پارتیشن ریشه با LVM در اوبونتو

این راهنما بر اساس خروجی‌های ثبت‌شده در گفتگو برای سرور `event-platform` با آدرس `10.1.60.52` تهیه شده است. اجرای موفق مراحل افزایش فضا هنوز در گفتگو تأیید نشده است.

## وضعیت فعلی سرور

| بخش | وضعیت ثبت‌شده |
| --- | --- |
| دیسک `/dev/sda` | ۴۰ گیگابایت |
| پارتیشن `/dev/sda3` در LVM | حدود ۳۶٫۹۵ گیگابایت، بدون فضای آزاد |
| ظرفیت دیسک `/dev/sdb` در `lsblk` | ۲۰۰ گیگابایت |
| ظرفیت شناخته‌شدهٔ `/dev/sdb` در LVM | کمتر از ۱۰۰ گیگابایت، بدون فضای آزاد |
| گروه حجم `ubuntu-vg` | حدود ۱۳۶٫۹۴ گیگابایت، بدون فضای آزاد |
| حجم منطقی `root` برای `/` | حدود ۱۱۸٫۴۷ گیگابایت |
| حجم منطقی `swap` | ۴ گیگابایت |
| حجم منطقی `var` برای `/var` | حدود ۱۴٫۴۷ گیگابایت |
| فایل‌سیستم `/` | `ext4`، اندازهٔ نمایشی ۱۱۷ گیگ، مصرف ۱۰۰ گیگ، فضای در دسترس ۱۲ گیگ، مصرف ۹۰٪ |
| فایل‌سیستم `/var` | `ext4`، اندازهٔ نمایشی ۱۵ گیگ، مصرف ۷۷۰ مگابایت، فضای در دسترس ۱۳ گیگ، مصرف ۶٪ |

## علت مشکل

سیستم‌عامل دیسک `/dev/sdb` را با ظرفیت ۲۰۰ گیگابایت می‌بیند، اما حجم فیزیکی LVM یا PV روی این دیسک هنوز با اندازهٔ حدود ۱۰۰ گیگابایت ثبت شده است. در نتیجه، حدود ۱۰۰ گیگابایت باقی‌مانده هنوز وارد فضای قابل تخصیص گروه حجم نشده است.

عدد ۱۱۸٫۵ گیگابایت در خروجی `lsblk` اندازهٔ کل حجم منطقی `root` است؛ این عدد میزان مصرف دیسک `sdb` نیست. نمایش `root` زیر هر دو دیسک یعنی این حجم منطقی از هر دو PV فضا گرفته است؛ نباید عدد آن را دوبار جمع کرد.

مسیر افزایش ظرفیت چنین است:

`دیسک /dev/sdb ← حجم فیزیکی LVM ← گروه ubuntu-vg ← حجم root ← فایل‌سیستم /`

## بررسی پیش از تغییر

دستورها را روی همان سرور لینوکسی اجرا کنید. پیش از تغییر، از داده‌های مهم بکاپ معتبر داشته باشید.

```bash
lsblk
sudo pvs
sudo vgs
sudo lvs
df -hT / /var
```

این راهنما برای وضعیتی است که `lsblk` ظرفیت `/dev/sdb` را ۲۰۰ گیگابایت و `pvs` خودِ `/dev/sdb` را عضو `ubuntu-vg` نشان می‌دهد. در خروجی ثبت‌شده، PV مستقیماً روی دیسک است و پارتیشنی مانند `/dev/sdb1` وجود ندارد؛ بنابراین افزایش پارتیشن لازم نیست.

## مرحلهٔ اول: شناسایی ظرفیت جدید در LVM

```bash
sudo pvresize /dev/sdb
sudo pvs
sudo vgs
```

پس از موفقیت:

- ظرفیت PV مربوط به `/dev/sdb` باید به نزدیک ۲۰۰ گیگابایت برسد.
- ستون `VFree` برای `ubuntu-vg` باید حدود ۱۰۰ گیگابایت فضای آزاد نشان دهد.
- در این مرحله هنوز اندازهٔ فایل‌سیستم `/` تغییر نمی‌کند.

اگر دستور خطا داد یا فضای آزاد اضافه نشد، مرحلهٔ بعد را اجرا نکنید؛ خروجی همین دستورها باید بررسی شود.

## مرحلهٔ دوم: اختصاص فضای آزاد به ریشه

برای اختصاص تمام فضای آزاد گروه حجم به `/`:

```bash
sudo lvextend -r -l +100%FREE /dev/ubuntu-vg/root
```

معنی گزینه‌ها:

- `-l +100%FREE`: تمام فضای آزاد موجود در گروه حجم را به حجم منطقی اضافه می‌کند.
- `-r`: همراه با افزایش حجم منطقی، فایل‌سیستم را نیز بزرگ می‌کند.

این دستور تمام فضای آزاد **گروه حجم** را مصرف می‌کند. مطابق وضعیت فعلی، فضای جدید از بزرگ‌شدن PV روی `/dev/sdb` به دست می‌آید. اگر قصد دارید بخشی از فضا برای افزایش آیندهٔ `/var` یا ساخت حجم دیگری باقی بماند، نباید تمام فضای آزاد را به `root` اختصاص دهید.

## مرحلهٔ سوم: بررسی نتیجه

```bash
sudo pvs
sudo vgs
sudo lvs
df -hT / /var
```

نتیجهٔ مورد انتظار:

- اندازهٔ حجم منطقی `root` از حدود ۱۱۸٫۴۷ به حدود ۲۱۸٫۴۷ گیگابایت می‌رسد.
- اندازهٔ فایل‌سیستم `/` در `df` افزایش پیدا می‌کند و درصد مصرف کاهش می‌یابد.
- پس از تخصیص تمام فضای آزاد، `VFree` دوباره صفر می‌شود؛ این رفتار طبیعی است.
- اندازهٔ حجم‌های `var` و `swap` تغییر نمی‌کند.

عدد نمایش‌داده‌شده در `df` ممکن است به علت سربار فایل‌سیستم، واحد نمایش و گردکردن، کمی با اندازهٔ حجم منطقی در `lvs` تفاوت داشته باشد.

## نکات مهم

- افزایش فایل‌سیستم `ext4` فعلی معمولاً هنگام روشن‌بودن سرور و در حالت mounted قابل انجام است و برای این مراحل نیازی به ریبوت نیست.
- فضای آزاد داخل فایل‌سیستم `/var` با فضای آزاد LVM متفاوت است. ۱۳ گیگابایت فضای در دسترس `/var` را نمی‌توان مستقیماً با دستور بالا به `/` اضافه کرد.
- برای این کار از `pvcreate`، `mkfs`، حذف پارتیشن یا فرمت دیسک استفاده نکنید؛ PV و فایل‌سیستم از قبل موجود هستند.
- اگر `lvextend -r` خطا داد، ممکن است افزایش حجم منطقی انجام شده ولی افزایش فایل‌سیستم کامل نشده باشد. پیش از اقدام بعدی، خروجی خطا، `sudo lvs` و `df -hT /` را بررسی کنید.
- این راهنما مخصوص نام دیسک‌ها و حجم‌های همین سرور است؛ روی سرور دیگر ابتدا نام‌ها و وضعیت واقعی را بررسی کنید.

## دستورهای اصلی در یک نگاه

هر مرحله را پس از بررسی موفقیت مرحلهٔ قبلی اجرا کنید:

```bash
# ۱. افزایش ظرفیت PV و بررسی فضای آزاد
sudo pvresize /dev/sdb
sudo vgs

# ۲. فقط در صورت مشاهدهٔ حدود ۱۰۰ گیگابایت فضای آزاد
sudo lvextend -r -l +100%FREE /dev/ubuntu-vg/root

# ۳. بررسی اندازهٔ نهایی
df -hT /
sudo lvs
sudo vgs
```
